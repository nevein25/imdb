@model IEnumerable<neveinNadeen.Models.Director>


@{
    /**/

    ViewBag.Title = "Directors";
}


<!DOCTYPE html>
<html>
<head>
    <link href="~/Styles/Index.css" rel="stylesheet" />
</head>
<body>
    <form class="form-inline" style="">
        @using (Html.BeginForm("Index", "Director", FormMethod.Get))
        {

            <div style="">
                <input id="searching" name="searching" class="form-control mr-sm-2" type="text" placeholder="Search IMDb" aria-label="Search" style="">
                <input class="btn btn-outline-warning" type="submit" value="Search" style="">

            </div>

        }
    </form>

    @{
        string name = "";

    }


    <table class="table table-striped">

        @foreach (var item in Model)
        {
            name = @Html.DisplayFor(modelItem => item.Fname).ToString() + " " + @Html.DisplayFor(modelItem => item.Lname).ToString();
            <tr>
                <td width="200" height="150">
                    <img src="~/images/directors/@item.Image" width="100" height="150" />
                </td>
                <td style="padding-top:75px ; font-size:20px">

                    <div class="small-box"><span class="name">@Html.ActionLink(name, "Details", "Director", new { id = item.Id }, new { style = "" })</span></div>
                </td>

            </tr>
        }

    </table>
</body>
</html>